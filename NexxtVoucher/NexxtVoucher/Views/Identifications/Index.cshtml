@model IEnumerable<NexxtVoucher.Models.Identification>

@{
    ViewBag.Title = @Resources.Resource.Identification_ViewIndex_Titulo1;
}

    <h2>@Resources.Resource.Identification_ViewIndex_Titulo1</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoDocumento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDocumento)
        </td>
        <td>
            @Html.ActionLink(@Resources.Resource.ButtonEdit, "Edit", new { id=item.IdentificationId }, new { @class="btn btn-warning btn-sm"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDetail, "Details", new { id=item.IdentificationId }, new { @class="btn btn-info btn-sm"}) 
            @*@Html.ActionLink(@Resources.Resource.ButtonDelete, "Delete", new { id=item.IdentificationId }, new { @class="btn btn-danger btn-sm"})*@
        </td>
    </tr>
}

</table>

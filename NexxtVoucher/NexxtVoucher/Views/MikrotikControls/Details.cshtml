@model NexxtVoucher.Models.MikrotikControl

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>@Resources.Resource.MikrotikControl_View_Titulo</h4>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.Server.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Server.Nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.PuertoApi)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PuertoApi)
        </dd>

    </dl>
</div>
<p>
    <button type="button" class="btn btn-success btn-sm" onclick="location.href = '@Url.Action("Index", new  {})'">
        <i class="fa fa-backspace"></i>
        @Resources.Resource.ButtonBack
    </button>

    <button type="submit" class="btn btn-warning btn-sm" onclick="location.href = '@Url.Action("Edit", new { id = Model.MikrotikControlId })'">
        <i class="fa fa-pencil-alt"></i>
        @Resources.Resource.ButtonEdit
    </button>
</p>

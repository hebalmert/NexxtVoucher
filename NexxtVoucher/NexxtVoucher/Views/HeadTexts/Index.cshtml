@model IEnumerable<NexxtVoucher.Models.HeadText>

@{
    ViewBag.Title = @Resources.Resource.TextHead_ViewIndex_Titulo1;
}

    <h2>@Resources.Resource.TextHead_ViewIndex_Titulo1</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TextoEncabezado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TextoEncabezado)
        </td>
        <td>
            @Html.ActionLink(@Resources.Resource.ButtonEdit, "Edit", new { id=item.HeadTextId }, new { @class="btn btn-warning btn-sm"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDetail, "Details", new { id=item.HeadTextId }, new { @class="btn btn-info btn-sm"}) 
            @*@Html.ActionLink("Delete", "Delete", new { id=item.HeadTextId })*@
        </td>
    </tr>
}

</table>

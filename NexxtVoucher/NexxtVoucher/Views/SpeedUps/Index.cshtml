@model IEnumerable<NexxtVoucher.Models.SpeedUp>

@{
    ViewBag.Title = @Resources.Resource.SpeedUp_Titualo;
}

    <h2>@Resources.Resource.SpeedUp_Titualo</h2>

<p>
    @Html.ActionLink(@Resources.Resource.ButtonNew, "Create", new { }, new { @class="btn btn-primary btn-sm"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VelocidadUp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VelocidadUp)
        </td>
        <td>
            @Html.ActionLink(@Resources.Resource.ButtonEdit, "Edit", new { id=item.SpeedUpId }, new { @class="btn btn-warning btn-sm"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDetail, "Details", new { id=item.SpeedUpId }, new { @class="btn btn-info btn-sm"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDelete, "Delete", new { id=item.SpeedUpId }, new { @class="btn btn-danger btn-sm"})
        </td>
    </tr>
}

</table>
